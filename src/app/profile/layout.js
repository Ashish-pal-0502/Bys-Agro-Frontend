export const metadata = {
  title: "My Profile | BYS Agro",
  description: "Manage your BYS Agro account details and saved addresses.",
  robots: { index: false, follow: false },
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}
